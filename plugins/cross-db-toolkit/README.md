# Cross-DB Toolkit

Claude Code plugin: engine-agnostic SQL analysis, rewriting and workload triage
that applies to both SQL Server and PostgreSQL.

Placeholder — components will be added as needed (e.g. `sql-rewrite`,
`diagnostics-intake`, `workload-classification`). Kept separate so shared,
engine-neutral logic does not get buried inside an engine-specific plugin.

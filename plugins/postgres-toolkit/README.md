# PostgreSQL Toolkit

Claude Code plugin: diagnostics, performance tuning and query design for
PostgreSQL.

Placeholder — components will be added as needed (e.g. an `explain-analysis`
skill and a log-preprocessing tool mirroring the SQL Server plugin). Kept as a
separate plugin so it can evolve and be published independently.
